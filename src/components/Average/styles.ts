import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    title: {
        color: '#fff',
        marginRight: 5
    },
    value: {
        color: '#FFF',
        fontWeight: 'bold',
        fontSize: 15,
        paddingTop: 2
    }
})